# 2.11 迁移 Worktree 状态

日期：2026-06-03

## 目的

记录 2.1.1 恢复基线冻结后，新建的 2.11 后端和前端迁移工作区。

这些 worktree 是后续迁移实验入口。它们和当前可运行的 2.1.1 环境隔离，避免破坏真实设备已经跑通的本地基线。

## 已冻结的 2.1.1 基线

后端恢复基线 tag：

```text
lsx-2.1.1-freeze-20260603
```

对应提交：

```text
cc183ccc chore: add local mqtt reproduction helpers
```

前端恢复基线 tag：

```text
lsx-ui-2.1.1-freeze-20260603
```

对应提交：

```text
e0234b59 feat: complete customer device recovery
```

这两个 tag 已经推送到私有远端。

## 后端 2.11 迁移工作区

路径：

```text
F:\project\other\jetlinks\worktrees\jetlinks-community-lsx-migration-2.11
```

分支：

```text
lsx-migration-2.11
```

远端：

```text
origin/lsx-migration-2.11
```

来源：

```text
origin/2.11
```

创建时 HEAD：

```text
a8141080 build(deps): bump org.bouncycastle:bcprov-jdk18on from 1.81 to 1.84 (#756)
```

基础版本：

```text
project.version      2.11.0-SNAPSHOT
spring.boot.version  3.4.10
java.version         17
```

本机默认 `java` 仍是 1.8，不能直接用于 2.11。当前可用候选 JDK：

```text
C:\Users\Administrator\.jdks\corretto-21.0.3
```

已验证 Maven wrapper 能在 JDK 21 下启动：

```powershell
$env:JAVA_HOME='C:\Users\Administrator\.jdks\corretto-21.0.3'
$env:Path="$env:JAVA_HOME\bin;$env:Path"
.\mvnw.cmd -version
```

输出确认：

```text
Apache Maven 3.9.3
Java version: 21.0.3
```

后续正式验证建议先跑：

```powershell
$env:JAVA_HOME='C:\Users\Administrator\.jdks\corretto-21.0.3'
$env:Path="$env:JAVA_HOME\bin;$env:Path"
.\mvnw.cmd -DskipTests package
```

## 前端 2.11 迁移工作区

路径：

```text
F:\project\other\jetlinks\worktrees\jetlinks-ui-vue-lsx-ui-migration-2.11
```

分支：

```text
lsx-ui-migration-2.11
```

远端：

```text
origin/lsx-ui-migration-2.11
```

来源：

```text
origin/2.11
```

创建时 HEAD：

```text
f5f95d8f fix: 更新@jetlinks-web/components版本
```

基础版本：

```text
package.version  2.11.0
node             v20.19.2
pnpm             10.14.0
```

当前 worktree 没有 `node_modules`。后续正式验证建议先跑：

```powershell
pnpm.cmd install
pnpm.cmd run build
```

## 与现有目录的关系

不要在下面两个目录里直接做 2.11 迁移实验：

```text
F:\project\other\jetlinks\jetlinks-community
F:\project\other\jetlinks\jetlinks-ui-vue-2.1.1
```

它们的作用是保留 2.1.1 后端和前端恢复证据。

另外，已有 2.11 前端目录：

```text
F:\project\other\jetlinks\jetlinks-ui-vue
```

当前存在未提交改动：

```text
M package.json
M pnpm-lock.yaml
M src/auto-imports.d.ts
M src/views/account/center/components/PersonalToken/components/PermissionList.vue
```

因此前端 2.11 迁移不要复用该目录，避免把旧的未提交改动混入迁移分支。

## 当前迁移入口

后端迁移从这里开始：

```text
F:\project\other\jetlinks\worktrees\jetlinks-community-lsx-migration-2.11
```

前端迁移从这里开始：

```text
F:\project\other\jetlinks\worktrees\jetlinks-ui-vue-lsx-ui-migration-2.11
```

迁移原则仍然是：

1. 2.1.1 本地环境作为对照组保留。
2. 2.11 worktree 作为实验区。
3. 每迁一个模块，都要用真实测试设备 `869624060285951` 做端到端验证。
4. 不把本地调试接口、本机 Vite 代理、生成文件当作业务迁移内容。
